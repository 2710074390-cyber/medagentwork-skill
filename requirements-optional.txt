# MedAgentWork Skill — 可选依赖清单
#
# 核心管线（validate_options / gate_check / workflow_state / qbank / fact_check /
# bloom_sampler / kaoyan_picker / render_qbank_html / render_review / contract_check）
# 只依赖 Python 标准库，无需安装任何东西。
#
# 本文件仅列出「可选增强」所需的第三方包。按需安装：
#     pip install -r requirements-optional.txt
#
# 说明（评审 §6.5-P2.9：可选依赖原先只散落在 README 与 SKILL.md 文字里）

# ── 配置解析（scripts/pipeline_config.py 读取 pipeline.yaml）──
# 缺失时自动回退到内置「最小解析器」（只解析扁平 thresholds: 段），仍可正常出题质检；
# 装上后可用完整 YAML 语法，并支持 pipeline.yaml 后续扩展为更复杂的结构。
PyYAML>=6.0

# ── 契约校验（contract_check.py）──
# 缺失时自动跳过 schema 校验（不阻断管线），装上后可校验 agent2/3/4 产物契约。
jsonschema>=4.0

# ── 中文分词（R10 / R11 结构规则的精度增强）──
# 缺失时规则回退到 n-gram 滑窗（仍可用，但关键词抽取精度略低）。
jieba>=0.42
